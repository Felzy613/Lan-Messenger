<UserControl
    x:Class="LanMessenger.UI.Sidebar.SidebarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LanMessenger.UI.Sidebar">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Conversation list -->
        <ListView x:Name="ConversationList"
                  Grid.Row="0"
                  SelectionMode="Single"
                  SelectionChanged="ConversationList_SelectionChanged">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="8,4" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ConversationRowViewModel">
                    <local:ConversationRowControl Row="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Empty state overlay -->
        <StackPanel x:Name="EmptyState"
                    Grid.Row="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="8"
                    Visibility="Collapsed">
            <TextBlock Text="No peers online"
                       HorizontalAlignment="Center"
                       Style="{ThemeResource SubtitleTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock Text="Looking for devices on your network…"
                       HorizontalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
        </StackPanel>

        <!-- Archived footer -->
        <Button x:Name="ArchivedSection"
                Grid.Row="1"
                Background="Transparent"
                BorderBrush="Transparent"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                Click="ArchivedSection_Click"
                Visibility="Collapsed"
                Padding="12,10">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <FontIcon Grid.Column="0"
                          FontFamily="Segoe MDL2 Assets"
                          Glyph="&#xE7B8;"
                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                <StackPanel Grid.Column="1" Spacing="2">
                    <TextBlock Text="Archived"
                               Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <TextBlock x:Name="ArchivedSubtitle"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <FontIcon Grid.Column="2"
                          FontFamily="Segoe MDL2 Assets"
                          Glyph="&#xE76C;"
                          FontSize="12"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
            </Grid>
        </Button>
    </Grid>
</UserControl>
