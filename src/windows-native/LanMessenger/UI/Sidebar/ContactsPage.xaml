<Page
    x:Class="LanMessenger.UI.Sidebar.ContactsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LanMessenger.UI.Sidebar"
    xmlns:ui="using:LanMessenger.UI">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Search bar -->
        <Border Grid.Row="0"
                Padding="12,10,12,6">
            <Grid ColumnSpacing="8"
                  Padding="10,6"
                  Background="{ThemeResource ControlFillColorDefaultBrush}"
                  CornerRadius="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <FontIcon Grid.Column="0"
                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                          Glyph="&#xE721;"
                          FontSize="14"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBox x:Name="SearchBox"
                         Grid.Column="1"
                         PlaceholderText="Search by name or IP"
                         BorderThickness="0"
                         Background="Transparent"
                         TextChanged="SearchBox_TextChanged">
                    <TextBox.Resources>
                        <SolidColorBrush x:Key="TextControlBorderBrushFocused"     Color="Transparent"/>
                        <SolidColorBrush x:Key="TextControlBorderBrushPointerOver" Color="Transparent"/>
                        <SolidColorBrush x:Key="TextControlBackgroundFocused"      Color="Transparent"/>
                        <SolidColorBrush x:Key="TextControlBackgroundPointerOver"  Color="Transparent"/>
                    </TextBox.Resources>
                </TextBox>
                <Button x:Name="ClearSearchBtn"
                        Grid.Column="2"
                        Width="24" Height="24"
                        Padding="0"
                        CornerRadius="12"
                        BorderThickness="0"
                        Background="Transparent"
                        Visibility="Collapsed"
                        Click="ClearSearch_Click">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                              Glyph="&#xE711;"
                              FontSize="10"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </Button>
            </Grid>
        </Border>

        <!-- Summary bar: "N contacts · M online" on the left, green "New contact" on the right -->
        <Grid Grid.Row="1"
              Padding="16,4,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="SummaryText"
                       Grid.Column="0"
                       Style="{ThemeResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center" />

            <Button Grid.Column="1"
                    x:Name="NewContactBtn"
                    Padding="10,5"
                    CornerRadius="6"
                    BorderThickness="0"
                    Click="AddFromPeers_Click"
                    ToolTipService.ToolTip="Find peers on your network and add them as contacts">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackground"             Color="#25D366"/>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver"  Color="#1EBE5D"/>
                    <SolidColorBrush x:Key="ButtonBackgroundPressed"      Color="#1AAD54"/>
                    <SolidColorBrush x:Key="ButtonForeground"             Color="White"/>
                    <SolidColorBrush x:Key="ButtonForegroundPointerOver"  Color="White"/>
                    <SolidColorBrush x:Key="ButtonForegroundPressed"      Color="White"/>
                    <SolidColorBrush x:Key="ButtonBorderBrush"            Color="Transparent"/>
                    <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="Transparent"/>
                    <SolidColorBrush x:Key="ButtonBorderBrushPressed"     Color="Transparent"/>
                </Button.Resources>
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                              Glyph="&#xE8FA;"
                              FontSize="11"
                              Foreground="White" />
                    <TextBlock Text="New contact"
                               FontSize="12"
                               Foreground="White" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Contact list — items are built programmatically so we can insert
             section headers (Online / Offline) without a CollectionViewSource. -->
        <ListView x:Name="ContactsList"
                  Grid.Row="2"
                  SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding"                    Value="0" />
                    <Setter Property="MinHeight"                  Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Empty state: no contacts, or no search matches -->
        <StackPanel x:Name="EmptyState"
                    Grid.Row="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="10"
                    Visibility="Collapsed">
            <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                      Glyph="&#xE779;"
                      FontSize="44"
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="EmptyTitle"
                       Text="No saved contacts"
                       Style="{ThemeResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="EmptySubtitle"
                       Text="Click &quot;New contact&quot; to find peers on your network."
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
        </StackPanel>
    </Grid>
</Page>
